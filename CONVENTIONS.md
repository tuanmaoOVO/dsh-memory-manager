# 委派-验证-复盘闭环规约

> 本文件是主 agent 与用户约定的工作规约（v1，2025 生效）。
> 每次处理具体问题时按本规约执行；复盘结果存记忆库（memory_save），不写入本文件。

## 目的

把"处理具体问题"的流程固化为固定纪律：**主 agent 只做四件事——打包、委派、验证、复盘入库**。子 agent 负责独立解决，主 agent 负责把关与沉淀。复盘反哺下一次打包，形成闭环。

## 核心原则

1. **自包含**：委派给子 agent 的必须是自包含任务包，不依赖主会话上下文。
2. **只收结果**：主 agent 不参与子 agent 内部过程，只接收最终结果并验证。
3. **验证先行**：任何结果必须按任务包中写明的验证标准核对通过后才算完成。
4. **复盘压缩**：每次验证通过后写 ≤200 字的紧凑复盘，不占主上下文。
5. **检索先行（经验库）**：处理问题打包前，先 memory_search 检索记忆库（标题+印象+预览），判断是否有可复用的经验（尤其历史复盘）；命中则并入任务包「相关经验」段。子 agent 处理实际问题前也应检索复盘记忆参考。记忆库持续沉淀即经验库，复盘/对话中发现旧记忆过时即可更新，不断成长。

## 五阶段流程

| 阶段 | 动作 | 工具 |
|---|---|---|
| 1. 打包 Pack | **先检索记忆库相关经验**，再整理问题背景为自包含任务包（含「相关经验」段） | memory_search + 任务包模板 |
| 2. 委派 Delegate | 后台派发任务包；独立子问题并行派发 | subagent（后台默认） |
| 3. 验证 Verify | 按验证标准用真实工具核对结果 | read / grep / pwsh 等 |
| 4. 复盘 Retro | 验证通过后压缩本次处理为复盘；**复盘前检索旧记忆，发现过时内容在 notes 标注** | 复盘模板 |
| 5. 并入 Merge | 复盘存入记忆库，打标签供检索 | memory_save |

### 验证失败的处理

- 第一次失败 → send_message 给**同一个**子 agent 定向反馈（主 agent 不自己动手）。
- 连续失败 → 换新子 agent 重新打包委派；或升级为主 agent 亲自处理。
- 主 agent 亲自处理完成后，复盘照常写（记录"委派失败原因"为坑）。

## 委派任务包模板

每次委派按此结构写，保证自包含：

```markdown
## 任务
<一句话目标 + 预期产出>

## 相关经验（记忆库检索结果，处理前必读）
<主 agent 打包前 memory_search 命中的记忆：标题 + 记忆id + 摘要。
子 agent 处理前应再自行 memory_search / memory_recall 检索相关复盘记忆（工具可用），
把实际参考了哪些经验写入过程简报>

## 背景
<问题由来、相关事实、约束条件、已尝试过的方案>
(任务包已自包含，无需访问主会话)

## 产出要求
最终回复必须包含:
1. 结论摘要 (≤5 行)
2. 验证证据 (如何证明结果是对的)
3. 变更清单 (改了什么/建了什么)
4. 过程简报 (≤10 行: 思路、尝试路径、遇到的坑、参考了哪些记忆库经验)   ← 复盘的数据来源
5. 遗留问题与风险

## 验证标准
<主 agent 将如何检查: 跑哪些测试、读哪些文件、满足什么条件才算通过>
```

## 复盘模板

验证通过后填写，直接 memory_save（impressions 打标签，如 `复盘`、`subagent`、问题领域关键词）：

```markdown
## 复盘 <问题名>
- 本质: <一句话>
- 解法: <路径概述>
- 关键决策: <1-3 条>
- 坑: <值得下次注意的>
- 可复用: <下次可直接用的知识点/模式>
```

## 边界规则

- 自包含且有体量的问题 → 走全套闭环。
- 琐碎单步任务 → 不委派，直接做。
- 一两个独立子问题 → 普通 subagent 并行。
- 大批量扇出 → workflow 脚本。
- 跨多轮长目标 → goal 工具管理主流程，每个具体问题仍走委派闭环。

## 工具映射速查

| 环节 | 工具 | 说明 |
|---|---|---|
| 委派 | subagent | 后台默认 |
| 反馈迭代 | send_message | 续用同一子 agent |
| 验证 | read / grep / pwsh 等 | 按验证标准逐条核对 |
| 复盘入库 | memory_save | impressions 打标签 |
| 热约定注入 | memory_pin | 短摘要自动注入 |
| 召回 | memory_search | 打包前先查历史复盘 |

## 跨会话使用（记忆管理插件，v0.2+）

规约记忆（tags 含 `convention` 且启用状态）由记忆管理插件自动管理：

- **新会话无条件常驻**：新会话创建时自动把启用中的规约记忆加入注入计划，每轮自动注入，无需任何口令。
- **旧会话非常驻**：恢复的旧会话不自动注入；需要时在记忆面板「记忆库 → 加入计划」，或直接说"把规约加入会话计划"（`session_inject`）。
- **记忆级开关**：每条记忆可在记忆库面板/编辑框切换"启用注入"；禁用后不参与自动注入、不能加入计划（已加入的也不再注入）。
- **全局开关**：设置 → 记忆管理 →「新会话自动注入规约记忆」可整体关闭。
- 备用：若记忆库未开启，直接读取本文件执行。

## 记忆库成长机制（经验库）

- **沉淀**：每次复盘 memory_save 入库（impressions 含 `复盘` 与领域关键词），复盘即经验。
- **检索**：打包前主 agent memory_search；子 agent 处理前自行 memory_search/memory_recall 参考复盘记忆（工具在子 agent 会话中可用）。
- **更新**：复盘或对话中发现旧记忆过时/错误 → 在记忆面板编辑该记忆（标题/印象/标签/正文标注），或新增更正记忆并在 notes 注明替代关系。记忆不是只写不读的档案，而是持续更新的经验。
- **打标签**：记忆的 tags 是分类标签（编辑框可改）；`convention` 标记规约记忆（参与新会话自动注入）。复盘记忆建议打领域标签，便于检索。

## 版本记录

- v1：规约建立（五阶段闭环、模板、边界规则、工具映射）。
- v2：新增"新会话启动口令"，配合记忆管理插件跨会话使用（记忆 m_oisgzkawkb）。
- v3：插件 v0.2 实现规约记忆自动常驻（新会话无条件注入、旧会话可选、记忆级启用开关），启动口令降级为备用手段。
- v4：检索先行强化——打包前/子 agent 处理前必检记忆库（相关经验段）；记忆更新成长机制（复盘/对话中更新旧记忆）；任务包模板新增「相关经验」段。
